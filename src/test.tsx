/** @format */

import React from 'react';

interface Greeting {
    name: string
}
const Test: React.FC<Greeting> = (props: Greeting) => {
    const a='wwwwwwww';
    return (
        <div>
        </div>
    );
};
export default Test;
